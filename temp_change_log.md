- Добавлено свойство "Color overylay" через которое можно накладывать цвет поверх всей текстуры
- Добавлена вкладка "Материал", в которой есть настройка цветов, настройка pbr текстур а так же поддержка разных материалов bobj/obj
- Дорожки костей на таймлайне теперь имеют иерархию, а не только отступы
- Дорожка текстуры стала как вкладка для материалов pbr
- Добавлена новая дорожка `color_overlay`
- Категории "Игрок" и "Модель" переименованы в "Запись" и "Форма"
- Панель иконок действий в редакторах теперь всегда находится сверху на полосе вкладок, как было ранее в редакторе фильмов
- Некоторые иконки из списка меню фильма вынесены как отдельные иконки в панели действий
- Аудио редактор теперь имеет вкладки как и любой другой редактор
- В редакторе фильмов, дорожки каждой части тела теперь как отдельные вкладки
- Дорожки-вкладки частей тела имеют превью кейфреймов внутри них
- Ограничители теперь имеют свои дорожки на каждую кость во вкладке позы
- Добавлены две новые кнопки в таймлайн: свернуть все и развернуть все секции. Они работают для секций частей тела
- Отрисовка гизмо теперь не зависит от FOV, это значит что при любом FOV гизмо будет выглядеть одинаково правильно
- Полностью обновлена панель "Морфинг". Основные изменения:
-- Теперь формы можно перемещать мышкой как drag&drop
-- Можно увеличивать/уменьшать ячейки с помощью `Ctrl + Колёсико мыши`
-- При наведении на ячейку есть полоса иконок быстрых действий
-- При двойном клике открывается редактор/завершается выбор
-- Возможность менять сортировку для каждой категории
- Полностью обновлена панель "Менеджер текстур". Основные изменения:
-- Текстуры теперь отрисовываются не списком а плитками
-- Слева теперь есть дерево папок
-- Возможно закреплять текстуры/папки над деревом папок
-- Breadcrumps
-- Управление файлами: копирование, удаление, дублирование, переименовывание и тд
-- Сочетания клавиш: Ctrl + Z (откатить), Ctrl + C & Ctrl + V и т.д.
-- Зум через `Ctrl + Колёсико мыши`
-- Система drag&drop
-- Панель свойств справа
-- Поиск
- `Сохранить как` в редакторе текстур теперь полноценная панель где можно выбирать папку и более удобно сохранять текстуру вместо поля ввода
- Родительское пространство было исправлено и теперь работает так, как родительское пространство
- Исправлено накопление физики при многократной трансформации родительской кости, а потом резкому сбросу
- Добавлен параметр "Вторичный цвет", который задает цвет фона
- Удалены параметры "Якрость фона" и "Белая тема", теперь они заменены вторичным цветом
- Контекстное меню теперь имеет горизонтальное меню иконок, которое заменяет пункты "Добавить ...", "Удалить ..." и т.д.
- Дочерние контекстные меню теперь имеют иконку "Назад"
- Обновлена начальная страница (новая вкладка). Теперь она стала красивее, имеет ссылки на дискорд/туториалы/вики а так же хранит в себе последние измененные объекты
- Новая панель "Оформление кейфрейма". Пкм по кейфрейм, и там вы сможете менять цвет/форму и "Заполнить" для кейфреймов
- Панель выбора цвета улучшена. Основные изменения:
-- Переключатель с HSV режима на RGB
-- Возможность добавлять избранные цвета
-- Добавлена пипетка по экрану
-- Два квадратика цвета, один отвечай за старый цвет для сравнения
-- Трекпады для точечного выбора цвета
-- Пресеты цветов (пкм по кнопка открытия панели)
- Настройка "Сторона полосы панелей" которая позволяет вам прикреплять панель дэшборда к другим крям экрана
- Удалены настройки "локальная трансформация по умолчанию" и "3D-луч для R/G/S"
- Исправлен баг, когда цепочки физики "подпрыгивали" при перемотки таймлайна
- Улучшен блок модели:
-- Свойство яркости блока
-- Свойство "Твёрдый"
-- Свойство "Столкновений с камерой"
-- Свойство прочности блока
-- Добавлена возможность менять звук ломания
-- Добавлена возможность менять хитбокс блока (куб, по форме, вручную)
-- Добавлена возможность надевать броню на модель
-- Панель стала с сплошным темным фоном и с вмозожностью менять ее размер
- Редактор анимаций текстур (`.mcmeta`) прямо в редакторе текстур. Основные изменения:
-- Кнопка "Анимация" в полосе действий включает/выключает анимацию текстуры; файл `.mcmeta` пишется рядом с PNG при сохранении
-- Под холстом появляется полоса кадров: кадры можно добавлять, дублировать, удалять, перетаскивать (порядок показа) и задавать каждому свою длительность
-- Холст показывает один кадр; листать кадры: `,` и `.`, новый кадр: `Alt + N`
-- Проигрывание прямо в редакторе и луковая кожа (предыдущий кадр просвечивает под текущим)
-- Превью модели анимируется вместе с текстурой
-- Секция "Анимация" в настройках: тиков на кадр, ширина/высота кадра
-- Все операции с кадрами отменяются через `Ctrl + Z`
-- Редактор не отходит от формата `.mcmeta`: что было в файле, то в нём и остаётся, меняются только `frametime`, `width`, `height` и `frames`
-- Пкм по текстуре в менеджере: "Сделать анимированной..." вместо создания пустого `.mcmeta`
-- Панель свойств показывает число кадров и длину анимации в тиках
-- Выделение переезжает вместе с кадром при листании
-- `Ctrl + Z` / `Ctrl + Y` работают по всему редактору, а не только над холстом
-- `+` и `Alt + N` создают копию текущего кадра (с Shift — пустой), `Ctrl + Колёсико мыши` листает кадры, `Пробел` — проиграть/пауза, `Shift + ,` / `Shift + .` — первый/последний кадр, Ctrl + перетаскивание кадра — копия
-- Пкм по кадрам: "Обратить порядок", "Пинг-понг" (обратный ход из тех же картинок, такие кадры помечены цепочкой), "Очистить кадр", отразить по горизонтали/вертикали
-- Шахматный фон в ячейках кадров той же яркости, что задана ползунком "Яркость" для холста
- "Макросы" в полосе действий редактора текстур (иконка со списком): очистить (`Delete`), отразить по горизонтали (`Shift + H`) и по вертикали (`Shift + V`) — для выделения или всего кадра на активном слое
- Превью модели в редакторе текстур показывает тот же кадр, что и холст, и играет синхронно с таймлайном
- Анимированные текстуры воспроизводятся в превью менеджера текстур (плитки и панель свойств), даже когда игра на паузе под панелью
- Исправлено: файл проекта редактора текстур (`.dat` со слоями) не переезжал вместе с текстурой при переименовании, перемещении, копировании и удалении
- У гизмо больше нет режимов: оно всегда содержит все элементы сразу, а что показывать — задаётся в настройках
-- В настройках "Трансформация" появилась строка "Элементы гизмо" — полоска из пяти иконок-тумблеров: перемещение, масштаб, вращение, экранное вращение и 3D-сфера
-- Скрытый элемент пропадает и из-под курсора, но клавиши G/S/R по-прежнему проходят по всем шагам — их порядок настраивается отдельно ("Цикл клавиши ...")
-- Настройки "3D сфера вращения" и "Скрыть кольца вращения" заменены новыми тумблерами, старые значения переносятся автоматически
-- Белый кубик в центре гизмо остаётся на месте, даже если выключить все элементы
-- Клавиша "Совмещённый" (T) убрана: переключать больше нечего
- Полосы иконок (режимы в редакторе частиц, грани в редакторе моделей, форма кейфрейма, элементы гизмо) читаются лучше: неактивные иконки приглушены, а ячейки больше не делят всю выданную ширину — раньше полоса из двух иконок в широкой колонке рисовала две ячейки по сотне пикселей с иконкой посередине каждой. Лишняя ширина теперь остаётся пустой, не ловит клики, а сама полоса прижимается к правому краю слота, вставая вровень с полями над и под ней
- Новая настройка "Размер плоскостей": насколько крупные у гизмо плоскости по двум осям (растут наружу от той же точки, что и раньше)
- "Фиксированный размер осей" переименован в "Фиксированный размер гизмо" и теперь влияет только на гизмо и превью осей вторичной кости, которое нарочно повторяет его размер. Метка центра орбиты и траектории движения держат постоянный размер на экране всегда
- В редакторе частиц подпись "Режим ориентации к камере" встала в одну строку со своими иконками, а не над ними
- Исправлено: если у гизмо оставить включённой только 3D-сферу, подсветка сферы под курсором пропадала до тех пор, пока не включишь и не выключишь обратно кольца вращения